---
- hosts: all
  become: true
  become_method: su
  gather_facts: false
  tasks:
    - lineinfile:
        path: /etc/sudoers
        state: present
        regex: '^ansible'
        line: 'ansible ALL=(ALL) NOPASSWD: ALL'
        validate: 'visudo -cf %s'
